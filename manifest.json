{
  "name": "cluck.in",
  "description": "Redirects clicked links on non-browsers to cluck.in, which prevents you from being tracked, and displays read-only of web pages.",
  "version": "0.1.0.5",
  "manifest_version": 2,

  "permissions": [
    "webRequest",
    "webRequestBlocking",
    "tabs",
    "http://*/*",
    "https://*/*"
  ],

  "background": {
    "scripts": ["background.js"]
  },

  "browser_action": {
    "default_title": "C.I",
    "default_icon": "icon.png",
    "default_popup": "popup.html"
  },

  "content_security_policy": "script-src 'self' https://ajax.googleapis.com; object-src 'self'"

}
